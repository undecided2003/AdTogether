rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /ads/{userId}/{allPaths=**} {
      // Allow anyone to read ad images
      allow read: if true;
      // Only the owner can upload/modify images in their folder
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
