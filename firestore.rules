rules_version = '2';

service cloud.firestore {

  match /databases/{database}/documents {

    match /{collectionName}/{document=**}{

      allow read: if collectionName != "config"

      allow write: if true 

    }
    match /config/{document=**} {

      allow read: if request.auth != null;

    }

  }

}
